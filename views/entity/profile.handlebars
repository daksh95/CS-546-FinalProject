<nav class="navbar navbar-expand-sm bg-primary navbar-dark justify-content-center sticky-top">
    <div class="container-fluid">
        <div class="navbar-header text-light">
            WebSite Name
        </div>
        <ul class="nav navbar-nav">
            <li class="nav-item"><a href="/land" class="nav-link">Home</a></li>
            <li class="nav-item"><a href="/user/{{userId}}/land" class="nav-link">View all transactions</a></li>
            <li class="nav-item"><a href="/user/{{userId}}/transactions" class="nav-link">View pending transactions</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li class="nav-item"><a href="/user/{{userId}}/profile" class="nav-link">Profile</a></li>
        </ul>
    </div>
</nav>
<header>
    <h1>Profile</h1>
</header>
<main>
    <div class="container">
        {{#if createdProfile}}
        <a href="#"><button class="btn btn-success pull-right">Edit Profile</button></a>
        <p>ID: {{entity._id}}</p>
        <p>Name: {{entity.name}}</p>
        <p>Role: {{entity.role}}</p>
        <p>Contact information: {{entity.contactInfo}}</p>
        <p>EmailId: {{entity.emailId}}</p>
        <p>Website: {{entity.Website}}</p>
        <p>License: {{entity.license}}</p>
        {{else}}
        <a href="#"><button class="btn btn-success pull-right">Create profile</button></a>
        {{/if}}
    </div>
</main>